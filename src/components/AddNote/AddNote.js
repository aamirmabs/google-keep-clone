import React, { useState } from "react";

import "./AddNote.css";

const AddNote = (props) => {
  return (
    <div>
      <h1>Add Note Component</h1>
    </div>
  );
};

export default AddNote;
